# npm's strict peer-dependency resolution (arborist) crashes on this
# workspace's dependency graph with "Cannot read properties of null
# (reading 'edgesOut')" in #loadPeerSet — a known npm bug, not a real
# conflict in our deps. This makes every install (including `npm ci` in CI)
# use the older, non-crashing resolution behavior.
legacy-peer-deps=true
