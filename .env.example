# Used locally by the Vite dev-server proxy (apps/*/vite.config.mts) and, in
# production, by the api/search.ts Vercel Edge Function — set the same key
# as a Vercel Environment Variable for the deployed project, it's not read
# from this file there.
API_TOKEN=
